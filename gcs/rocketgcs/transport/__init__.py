"""Byte transport (G4): serial/SiK radio + wired-UART debug path.

Transport-agnostic interface above the wire (D028/D029) so the protocol layer
does not care whether bytes arrive over the radio or the wired link.
"""
